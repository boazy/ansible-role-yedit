name: Release to Galaxy

on:
  - publish

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: galaxy
        uses: robertdebock/galaxy-action@1.2.0
        with:
          galaxy_api_key: ${{ secrets.SAF_ANSIBLE_GALAXY_API_TOKEN }}